package me.cosmodev.utils.db.mysql;

public enum DatabaseResourceType {
    ARRAY, STRING, INT, BIG_DECIMAL, BINARY_STREAM,
    BLOB, BOOLEAN, BYTE, BYTES, DATE,
    FLOAT, LONG, OBJECT, SHORT, URL;
}
